/*
 * mac.c — IEEE 802.15.4 MAC helpers
 *
 * All definitions are header-only (mac.h). This translation unit exists
 * so that the CMakeLists.txt source list resolves correctly.
 */

#include "mac.h"
